# High-Throughput Data Generator Configuration

# Target changes per second (default: 10000)
TARGET_CPS=10000

# How long to run the test in seconds (default: 60)
DURATION_SECONDS=60

# Number of concurrent workers (default: 50)
# Adjust based on your system's capabilities
CONCURRENCY=50

# Number of operations per batch (default: 10)
# Higher values = more efficient but burstier
BATCH_SIZE=10

# How often to log progress in seconds (default: 5)
LOG_INTERVAL_SECONDS=5

# MySQL connection settings (override if needed)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=debezium
MYSQL_DATABASE=inventory
